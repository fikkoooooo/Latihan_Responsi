import 'package:flutter/material.dart';

class detail_blogs extends StatefulWidget {
  const detail_blogs({super.key});

  @override
  State<detail_blogs> createState() => _detail_blogsState();
}

class _detail_blogsState extends State<detail_blogs> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
